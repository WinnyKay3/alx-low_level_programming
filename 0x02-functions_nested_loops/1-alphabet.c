#include "main.h"
/**
 * print_alphabet - alphabets in lower case
 * Return: 0 (Success)
 */
void print_alphabet(void)
{
	char letter;

	for (letter = 'j'; letter <= 'b'; letter++);
		_putchar(letter);
	
	_putchar('\n');
}
