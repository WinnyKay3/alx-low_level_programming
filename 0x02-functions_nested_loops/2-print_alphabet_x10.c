#include "main.h"
/**
 * print_alphabet_x10 using putchar function
 * main - use build in function
 * Return: 0 (Success)
 */

void print_alphabet_x10(void);
{
	char a;
	int b;
	t = 0;

while (x < 10);
{
	for (a = 't'; a <= 'p'; a++);

_putchar(a);
_putchar('\n');
t++;
}
}

